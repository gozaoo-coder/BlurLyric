# 06 - 实施计划

## 6.1 开发阶段规划

### 6.1.1 阶段概览

| 阶段 | 名称 | 周期 | 主要目标 | 状态 |
|------|------|------|----------|------|
| P0 | 基础架构 | 已完成 | 核心框架搭建 | ✅ 已完成 |
| P1 | 本地音乐功能 | 已完成 | 本地音乐管理 | ✅ 已完成 |
| P2 | 性能优化 | 已完成 | 缓存和性能优化 | ✅ 已完成 |
| P3 | 网络音乐源 | 2周 | 网络源接入 | 🔄 进行中 |
| P4 | 用户偏好系统 | 2周 | 歌单和收藏 | 🔄 规划中 |
| P5 | UI完善与测试 | 2周 | 界面优化和测试 | 🔄 规划中 |

---

## 6.2 已完成阶段详情

### 6.2.1 P0 - 基础架构 (已完成)

**完成时间**: 项目初期

**交付物**:
| 交付物 | 状态 | 说明 |
|--------|------|------|
| Tauri项目框架 | ✅ | Tauri 2.0 + Vue.js |
| 前端组件库 | ✅ | 基础UI组件 |
| 后端模块结构 | ✅ | Rust模块化架构 |
| 构建配置 | ✅ | Vite + Cargo |

**关键文件**:
```
src-tauri/
├── Cargo.toml          # Rust依赖配置
├── tauri.conf.json     # Tauri配置
└── src/
    ├── lib.rs          # 主库入口
    └── main.rs         # 应用入口

src/
├── main.js             # Vue入口
├── App.vue             # 根组件
└── router/index.js     # 路由配置
```

### 6.2.2 P1 - 本地音乐功能 (已完成)

**完成时间**: 项目初期

**交付物**:
| 交付物 | 状态 | 说明 |
|--------|------|------|
| 音乐目录管理 | ✅ | 添加/删除/扫描目录 |
| 元数据解析模块 | ✅ | music_tag模块 |
| 音乐列表展示 | ✅ | 曲目列表组件 |
| 专辑/艺人视图 | ✅ | 专辑和艺人页面 |

**关键文件**:
```
src-tauri/src/
├── music_tag/          # 元数据解析模块
│   ├── mod.rs
│   ├── parser.rs
│   ├── types.rs
│   └── error.rs
└── lib.rs              # IPC命令

src/
├── api/source/         # 数据源
│   ├── base.js
│   └── tauri.js
├── api/resources/      # 资源管理
│   └── index.js
└── router/routers/     # 页面组件
    ├── allMusic.vue
    ├── allLocalAlbum.vue
    └── allLocalArtist.vue
```

### 6.2.3 P2 - 性能优化 (已完成)

**完成时间**: 项目初期

**交付物**:
| 交付物 | 状态 | 说明 |
|--------|------|------|
| 增量扫描器 | ✅ | 基于文件指纹 |
| 多级缓存系统 | ✅ | 内存+磁盘缓存 |
| GPU图片处理 | ✅ | 封面缩放加速 |
| 音乐去重 | ✅ | 智能合并 |
| 性能监控 | ✅ | 全面的指标记录 |

**关键文件**:
```
src-tauri/src/
├── music_library_cache.rs # 音乐库缓存（本地音乐元数据）
├── resource_cache.rs      # 资源缓存（网络资源）
├── incremental_scanner.rs # 增量扫描
├── gpu_image_processor.rs # GPU处理
├── image_processor.rs     # 图片处理
├── music_deduplicator.rs  # 去重模块
└── performance_monitor.rs # 性能监控
```

**缓存架构说明**:

本项目使用两套缓存系统，职责分明：

| 缓存系统 | 模块名 | 职责 |
|---------|--------|------|
| 音乐库缓存 | `music_library_cache` | 本地音乐文件元数据缓存 |
| 资源缓存 | `resource_cache` | 网络下载的音频、图片等资源文件 |

**音乐库缓存 (MusicLibraryCache)**:
- 缓存内容：歌曲名、艺人、专辑、时长等元数据
- 数据来源：本地文件扫描
- 清理策略：增量扫描时更新
- 用户交互：无感知，自动管理

**资源缓存 (ResourceCache)**:
- 缓存内容：网络下载的音频、图片等资源文件
- 数据来源：网络下载
- 清理策略：LRU 自动清理
- 用户交互：用户可手动管理
- 支持分层：临时池（试听）+ 偏好池（收藏）

---

## 6.3 待实施阶段详情

### 6.3.1 P3 - 网络音乐源 (进行中)

**预计周期**: 2周

**目标**: 实现网络音乐源的接入和统一管理

**任务分解**:

| 任务ID | 任务描述 | 预计工时 | 优先级 | 依赖 | 状态 |
|--------|----------|----------|--------|------|------|
| P3-001 | 实现Trace来源追踪机制 | 8h | 高 | 无 | ✅ 已完成 |
| P3-002 | 实现ApiSource抽象类 | 8h | 高 | P3-001 | ✅ 已完成 |
| P3-003 | 实现SourceManager多源管理 | 8h | 高 | P3-002 | ✅ 已完成 |
| P3-004 | 实现网络搜索功能 | 8h | 高 | P3-003 | ✅ 已完成 |
| P3-005 | 实现缓存分层架构 | 8h | 高 | P3-001 | ✅ 已完成 |
| P3-006 | 实现资源调用流程 | 8h | 高 | P3-005 | ✅ 已完成 |
| P3-007 | 实现网络源配置UI | 8h | 中 | P3-003 | ⏳ 待开始 |
| P3-008 | 集成测试 | 4h | 高 | P3-001~P3-007 | ⏳ 待开始 |

**已完成工作**:
- ✅ Trace 来源追踪机制完整实现（前端 + 后端）
  - 前端：`src/api/source/trace.js` - Trace 类完整实现
  - 后端：`src-tauri/src/trace.rs` - Trace 结构体 + BaseModel trait
  - 支持多种来源类型（Storage/API）
  - 支持多种获取方法（LocalFile/Download/Stream/ApiCall）
- ✅ ApiSource 抽象类完整实现
  - 前端：`src/api/source/api.js` - ApiSource 类
  - 前端：`src/api/source/base.js` - Source 基类
  - 搜索/详情/歌单/资源获取接口定义完整
  - 缓存管理机制实现
- ✅ SourceManager 多源管理实现
  - 前端：`src/api/source/index.js` - SourceManager 类
  - 多源注册和管理
  - Trace 导航支持
  - 向后兼容代理方法
- ✅ 缓存分层架构完整实现（已重命名为 ResourceCache）
  - 后端：`src-tauri/src/resource_cache.rs` - ResourceCacheManager
  - 前端：`src/api/resourceCache.js` - ResourceCache 类
  - 临时缓存池 + 用户偏好池
  - LRU 清理策略
  - 缓存索引持久化
  - IPC 命令完整注册
- ✅ 音乐库缓存管理器重命名完成
  - 后端：`src-tauri/src/music_library_cache.rs` - MusicLibraryCache
  - 前端：`src/api/libraryCache.js` - LibraryCache 类
  - 职责明确区分：音乐库缓存 vs 资源缓存
- ✅ 数据模型完整实现（前端 + 后端）
  - 前端：`src/api/resources/index.js` - Track/Artist/Album 类
  - 后端：`src-tauri/src/models/` - Track/Artist/Album 结构体
  - 所有模型实现 BaseModel trait
  - 所有模型包含 traces 字段
- ✅ 前端代码迁移完成（移除旧的 `sources` 属性，统一使用 `traces`）
- ✅ 右键菜单来源切换功能实现
- ✅ Bug 修复（menuConfig.js 重复声明问题）
- ✅ 资源调用流程完整实现（P3-006）
  - 前端：`src/api/resourceFetcher.js` - ResourceFetcher 类
  - 统一的缓存检查流程：缓存检查 → 下载 → 缓存 → 返回
  - Trace.fetchResource() 增强支持缓存
  - SourceManager 资源获取方法更新
  - 预加载和批量预加载支持
  - 后端 IPC 命令：`read_cached_file`
- ✅ 网络搜索功能完整实现（P3-004）
  - 前端：`src/api/source/netease.js` - NeteaseSource 适配器
  - 支持 NeteaseCloudMusicApiEnhanced API 接入
  - 搜索/详情/歌单/资源获取接口实现
  - 数据格式翻译和 Trace 生成
  - 多源搜索支持：SourceManager.searchAllSources()
  - 搜索 UI 支持源选择和结果统计
  - 搜索模块：`src/js/search/index.js` 更新

**详细任务说明**:

#### P3-001 实现Trace来源追踪机制

```rust
// Trace - 来源追踪信息
struct Trace {
    // 来源标识
    source_type: SourceType,       // Storage 或 Api
    source_id: String,             // 用户自定义标识
    source_name: String,           // 显示名称
    data_type: TraceDataType,      // Track/Artist/Album/Playlist
    
    // API源配置
    base_url: Option<String>,      // API基础地址
    
    // 数据标识
    data_id: String,               // 数据在该来源中的ID
    fetch_method: FetchMethod,     // LocalFile/Download/ApiCall
}

enum SourceType {
    Storage(StorageType),  // Local 或 WebDAV
    Api,                   // API型（不硬编码具体平台）
}
```

#### P3-002 实现ApiSource抽象类

```javascript
// src/api/source/api.js
class ApiSource extends Source {
    constructor(sourceId, sourceName, baseUrl) {
        super(sourceId, sourceName, 'api', SourceType.API);
        this.baseUrl = baseUrl;
    }
    
    // 搜索功能
    async searchTracks(keyword, options = {}) {
        const response = await fetch(
            `${this.baseUrl}/search?q=${encodeURIComponent(keyword)}&type=tracks`
        );
        return response.json();
    }
    
    // 创建Trace
    createTrace(dataType, dataId, fetchMethod) {
        return Trace.createApiSource(
            this.sourceId,
            this.sourceName,
            this.baseUrl,
            dataType,
            dataId,
            fetchMethod
        );
    }
}
```

#### P3-003 实现SourceManager多源管理

```javascript
// src/api/source/index.js
class SourceManager {
    #sources = new Map();
    
    registerSource(sourceId, source) {
        this.#sources.set(sourceId, source);
    }
    
    getSource(sourceId) {
        return this.#sources.get(sourceId);
    }
    
    // 根据Trace跳转到来源数据
    async navigateByTrace(trace) {
        const source = this.getSource(trace.sourceId);
        if (!source) {
            throw new Error(`Source not found: ${trace.sourceId}`);
        }
        return await source.getByTrace(trace);
    }
}
```

**验收标准**:
- [ ] Trace来源追踪机制正常工作
- [ ] 支持用户自定义网络源配置
- [ ] 搜索响应时间 < 3秒
- [ ] 缓存分层正常工作
- [ ] 资源调用流程完整

---

### 6.3.2 P4 - 用户偏好系统 (规划中)

**预计周期**: 2周

**目标**: 实现歌单管理、收藏功能和播放历史

**任务分解**:

| 任务ID | 任务描述 | 预计工时 | 优先级 | 依赖 |
|--------|----------|----------|--------|------|
| P4-001 | 设计用户偏好数据结构 | 4h | 高 | 无 |
| P4-002 | 实现歌单CRUD操作 | 8h | 高 | P4-001 |
| P4-003 | 实现"我喜欢"收藏功能 | 4h | 高 | P4-001 |
| P4-004 | 实现播放历史记录 | 4h | 中 | P4-001 |
| P4-005 | 实现偏好数据持久化 | 8h | 高 | P4-002~P4-004 |
| P4-006 | 歌单管理UI | 8h | 高 | P4-002 |
| P4-007 | 收藏功能UI | 4h | 高 | P4-003 |
| P4-008 | 播放历史UI | 4h | 中 | P4-004 |
| P4-009 | 歌单导入导出 | 4h | 低 | P4-002 |
| P4-010 | 集成测试 | 4h | 高 | P4-001~P4-009 |

**详细任务说明**:

#### P4-001 设计用户偏好数据结构

```rust
// 用户偏好数据
struct UserPreference {
    user_id: Option<String>,
    playlists: Vec<Playlist>,
    liked_tracks: Vec<u32>,
    play_history: Vec<PlayRecord>,
    created_at: u64,
    updated_at: u64,
}

// 歌单
struct Playlist {
    id: String,
    name: String,
    description: Option<String>,
    cover_url: Option<String>,
    track_ids: Vec<u32>,
    created_at: u64,
    updated_at: u64,
    is_system: bool,
}

// 播放记录
struct PlayRecord {
    track_id: u32,
    played_at: u64,
    play_duration: f64,
    source: String,
}
```

#### P4-002 实现歌单CRUD操作

```rust
// Tauri命令
#[tauri::command]
fn create_playlist(name: String, description: Option<String>) -> Result<Playlist, String>;

#[tauri::command]
fn update_playlist(id: String, name: Option<String>, description: Option<String>) -> Result<Playlist, String>;

#[tauri::command]
fn delete_playlist(id: String) -> Result<(), String>;

#[tauri::command]
fn add_tracks_to_playlist(playlist_id: String, track_ids: Vec<u32>) -> Result<(), String>;

#[tauri::command]
fn remove_tracks_from_playlist(playlist_id: String, track_ids: Vec<u32>) -> Result<(), String>;

#[tauri::command]
fn get_all_playlists() -> Result<Vec<Playlist>, String>;

#[tauri::command]
fn get_playlist_by_id(id: String) -> Result<Playlist, String>;
```

#### P4-003 实现"我喜欢"收藏功能

```rust
#[tauri::command]
fn like_track(track_id: u32) -> Result<(), String>;

#[tauri::command]
fn unlike_track(track_id: u32) -> Result<(), String>;

#[tauri::command]
fn is_track_liked(track_id: u32) -> Result<bool, String>;

#[tauri::command]
fn get_liked_tracks() -> Result<Vec<Song>, String>;
```

**验收标准**:
- [ ] 歌单创建/编辑/删除正常
- [ ] 收藏功能正常工作
- [ ] 播放历史记录正确
- [ ] 数据重启后保留

---

### 6.3.3 P5 - UI完善与测试 (规划中)

**预计周期**: 2周

**目标**: 完善用户界面，进行全面测试

**任务分解**:

| 任务ID | 任务描述 | 预计工时 | 优先级 | 依赖 |
|--------|----------|----------|--------|------|
| P5-001 | UI主题系统 | 8h | 中 | 无 |
| P5-002 | 深色模式支持 | 4h | 中 | P5-001 |
| P5-003 | 响应式布局优化 | 8h | 中 | 无 |
| P5-004 | 键盘快捷键 | 4h | 低 | 无 |
| P5-005 | 单元测试编写 | 16h | 高 | P3, P4 |
| P5-006 | 集成测试编写 | 8h | 高 | P5-005 |
| P5-007 | 性能测试 | 4h | 高 | P5-006 |
| P5-008 | Bug修复 | 8h | 高 | P5-005~P5-007 |
| P5-009 | 文档完善 | 4h | 中 | P5-008 |

**验收标准**:
- [ ] 单元测试覆盖率 > 70%
- [ ] 集成测试通过率 100%
- [ ] 性能测试达标
- [ ] 无P0/P1级别Bug

---

## 6.4 里程碑计划

### 6.4.1 里程碑定义

| 里程碑 | 名称 | 目标日期 | 验收标准 |
|--------|------|----------|----------|
| M1 | 架构完成 | 已完成 | 核心模块可编译运行 |
| M2 | 本地功能 | 已完成 | 可扫描播放本地音乐 |
| M3 | 网络功能 | 第2周末 | 可搜索播放网络音乐 |
| M4 | 用户系统 | 第4周末 | 歌单偏好功能完整 |
| M5 | 发布就绪 | 第6周末 | 通过测试，可发布 |

### 6.4.2 里程碑验收检查清单

#### M3 - 网络功能验收

- [ ] 网络源配置正常保存
- [ ] 网络搜索返回正确结果
- [ ] 网络音乐可正常播放
- [ ] 网络资源缓存正常工作
- [ ] 网络源状态可监控

#### M4 - 用户系统验收

- [ ] 歌单CRUD操作正常
- [ ] 收藏功能正常
- [ ] 播放历史记录正确
- [ ] 数据持久化正常
- [ ] UI交互流畅

#### M5 - 发布就绪验收

- [ ] 所有测试通过
- [ ] 性能指标达标
- [ ] 无严重Bug
- [ ] 文档完整
- [ ] 打包发布正常

---

## 6.5 资源需求

### 6.5.1 人力资源

| 角色 | 职责 | 投入比例 |
|------|------|----------|
| 全栈开发 | 前后端开发 | 100% |
| 测试 | 测试用例编写和执行 | 按需 |
| 产品 | 需求确认和验收 | 按需 |

### 6.5.2 技术资源

| 资源类型 | 说明 |
|----------|------|
| 开发环境 | Windows 11, Node.js, Rust |
| 测试环境 | 多平台测试机器 |
| CI/CD | GitHub Actions |
| 文档工具 | Markdown |

---

## 6.6 风险管理

### 6.6.1 进度风险

| 风险 | 可能性 | 影响 | 应对措施 |
|------|--------|------|----------|
| 网络源API变更 | 高 | 中 | 抽象接口，降低耦合 |
| 技术难点延期 | 中 | 高 | 预留缓冲时间 |
| 需求变更 | 中 | 中 | 敏捷迭代 |

### 6.6.2 质量风险

| 风险 | 可能性 | 影响 | 应对措施 |
|------|--------|------|----------|
| 测试覆盖不足 | 中 | 高 | 强制测试要求 |
| 性能不达标 | 中 | 高 | 持续性能监控 |
| 兼容性问题 | 中 | 中 | 多平台测试 |

---

## 6.7 发布计划

### 6.7.1 版本规划

| 版本 | 目标日期 | 主要功能 |
|------|----------|----------|
| v3.0.0 | 当前 | 本地音乐管理 |
| v3.1.0 | M3后 | 网络音乐源 |
| v3.2.0 | M4后 | 用户偏好系统 |
| v3.3.0 | M5后 | 完整功能发布 |

### 6.7.2 发布检查清单

- [ ] 所有功能测试通过
- [ ] 性能指标达标
- [ ] 文档更新完成
- [ ] 变更日志编写
- [ ] 版本号更新
- [ ] 打包构建成功
- [ ] 安装测试通过

---

## 6.8 文档版本

| 版本 | 日期 | 修改内容 |
|------|------|----------|
| 1.0 | 2026-03-29 | 初始版本 |
| 1.1 | 2026-03-29 | **重大更新**：网络源任务重构，基于Trace机制和合规性设计，移除硬编码平台名称 |
| 1.2 | 2026-03-29 | 更新P3阶段进度，标记已完成任务，添加下一步推荐方案 |
| 1.3 | 2026-03-29 | **重大更新**：P3-001、P3-002、P3-003 已全部完成，更新下一步推荐为 P3-005 缓存分层架构 |
| 1.4 | 2026-03-29 | **重大更新**：P3-005 缓存分层架构已完成，包含后端 CacheLayerManager 和前端 CacheLayer 类 |
| 1.5 | 2026-03-29 | **重大更新**：P3-004 网络搜索功能已完成，实现 NeteaseSource 适配器和多源搜索 |

---

## 6.9 下一步推荐方案

### 6.9.1 当前状态分析

**已完成**:
- ✅ P3-001: Trace 来源追踪机制（前端 + 后端完整实现）
- ✅ P3-002: ApiSource 抽象类（前端完整实现）
- ✅ P3-003: SourceManager 多源管理（前端完整实现）
- ✅ P3-004: 网络搜索功能（前端完整实现）
- ✅ P3-005: 缓存分层架构（前端 + 后端完整实现）
- ✅ P3-006: 资源调用流程（前端完整实现）
- ✅ 数据模型完整实现（前端 + 后端）
- ✅ 前端代码迁移（sources → traces）
- ✅ 右键菜单来源切换功能
- ✅ Bug 修复

**待完成**:
- ⏳ P3-007: 网络源配置UI
- ⏳ P3-008: 集成测试

### 6.9.2 推荐方案：实现 P3-007 网络源配置UI

**推荐理由**:
1. **功能完善**: 用户需要通过 UI 配置和管理网络源
2. **用户体验**: 提供可视化的源管理界面
3. **测试基础**: 为集成测试提供完整的用户流程

**实施方案**:

#### 步骤 1: 创建网络源配置页面

```vue
<!-- src/router/routers/sourceConfig.vue -->
<template>
  <div class="source-config">
    <h2>网络源配置</h2>
    
    <!-- 已配置的源列表 -->
    <div class="source-list">
      <div v-for="source in configuredSources" :key="source.id" class="source-item">
        <span>{{ source.name }}</span>
        <span>{{ source.baseUrl }}</span>
        <button @click="testSource(source.id)">测试连接</button>
        <button @click="removeSource(source.id)">删除</button>
      </div>
    </div>
    
    <!-- 添加新源 -->
    <div class="add-source">
      <input v-model="newSourceName" placeholder="源名称" />
      <input v-model="newSourceUrl" placeholder="API 地址" />
      <select v-model="newSourceType">
        <option value="netease">网易云音乐</option>
        <option value="api">自定义 API</option>
      </select>
      <button @click="addSource">添加源</button>
    </div>
  </div>
</template>
```

#### 步骤 2: 实现源配置持久化

```javascript
// 保存源配置到本地存储
const saveSourceConfig = (sources) => {
  localStorage.setItem('networkSources', JSON.stringify(sources));
};

// 加载源配置
const loadSourceConfig = () => {
  const saved = localStorage.getItem('networkSources');
  return saved ? JSON.parse(saved) : [];
};
```

### 6.9.3 替代方案：直接进行 P3-008 集成测试

**适用场景**: 如果网络源配置 UI 不急于实现

**优点**:
- 验证已实现功能的正确性
- 发现潜在问题
- 确保系统稳定性

**测试范围**:
1. Trace 来源追踪机制
2. 多源搜索功能
3. 资源获取和缓存
4. 来源切换功能
```

### 6.9.3 替代方案：先实现 P3-004 网络搜索功能

**适用场景**: 如果希望快速验证网络源接入流程

**优点**:
- 可以立即体验网络音乐搜索
- 验证 Trace 机制是否正常工作
- 为后续功能提供测试基础

**缺点**:
- 没有资源调用流程支持，无法完整播放网络音乐

### 6.9.4 建议执行顺序

1. **立即执行**: P3-006 资源调用流程（整合缓存）
2. **后续执行**: P3-004 网络搜索功能
3. **最后执行**: P3-007 网络源配置UI + P3-008 集成测试

### 6.9.5 关键实现要点

#### 缓存分层逻辑

```
用户搜索音乐 → 临时缓存池
     ↓
用户点击"喜欢"/加入歌单 → 移动到偏好池
     ↓
用户取消收藏/移出歌单 → 从偏好池移除（不管理）
     ↓
临时池满了 → LRU 清理最久未访问的缓存
```

#### 资源调用流程

```
用户播放音乐
     ↓
检查 Trace 来源
     ↓
本地文件？ → 直接播放
     ↓
缓存存在？ → 从缓存播放
     ↓
网络资源？ → 下载到临时缓存 → 播放
```

#### 合规性设计

- **不硬编码平台名称**: 所有网络源通过 `sourceId` 和 `baseUrl` 标识
- **通用接口规范**: 符合接口规范的网络源即可接入
- **用户自定义**: 用户可以自定义网络源名称和配置
