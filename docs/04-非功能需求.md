# 04 - 非功能需求

## 4.1 性能需求

### 4.1.1 启动性能

| 指标 | 目标值 | 测量方法 | 优先级 |
|------|--------|----------|--------|
| 冷启动时间 | < 3秒 | 从点击图标到可操作状态 | 高 |
| 热启动时间 | < 1秒 | 从后台恢复到可操作状态 | 高 |
| 首屏渲染时间 | < 500ms | 从启动到UI可见 | 中 |

**现有实现**:

```rust
// src-tauri/src/lib.rs
#[tauri::command]
fn init_application() {
    PerformanceMonitor::start_timer("init_application");
    
    // 1. 初始化缓存管理器
    CacheManager::init();
    
    // 2. 加载音乐目录
    load_music_dirs_from_disk();
    
    // 3. 尝试从缓存加载（快速启动）
    let cache_loaded = load_from_persistent_cache();
    
    if !cache_loaded {
        // 首次启动执行全量扫描
        refresh_music_cache();
        save_to_persistent_cache();
    } else {
        // 后台增量扫描
        perform_background_incremental_scan();
    }
    
    if let Some(duration) = PerformanceMonitor::end_timer("init_application") {
        println!("Application initialization completed in {}ms", duration);
    }
}
```

### 4.1.2 扫描性能

| 指标 | 目标值 | 测量方法 | 优先级 |
|------|--------|----------|--------|
| 全量扫描速度 | > 1000文件/分钟 | 扫描本地音乐库 | 高 |
| 增量扫描速度 | > 5000文件/分钟 | 增量更新检测 | 高 |
| 元数据解析速度 | > 200文件/分钟 | 解析ID3/Vorbis标签 | 中 |

**性能优化措施**:

1. **增量扫描**: 基于文件指纹的变更检测
2. **并行处理**: 多线程元数据解析
3. **缓存优先**: 优先使用持久化缓存
4. **后台更新**: 非阻塞的增量更新

### 4.1.3 运行时性能

| 指标 | 目标值 | 测量方法 | 优先级 |
|------|--------|----------|--------|
| 内存占用 | < 500MB | 正常运行时内存使用 | 高 |
| CPU占用 | < 5% | 空闲状态CPU使用率 | 中 |
| 本地搜索响应 | < 500ms | 搜索本地音乐库 | 高 |
| 网络搜索响应 | < 3秒 | 网络源搜索 | 中 |
| 封面加载时间 | < 200ms | 从缓存加载封面 | 中 |

### 4.1.4 资源管理性能

| 指标 | 目标值 | 测量方法 | 优先级 |
|------|--------|----------|--------|
| 缓存命中率 | > 80% | 封面缓存命中统计 | 中 |
| 资源释放延迟 | < 100ms | 资源释放响应时间 | 低 |
| GPU处理加速比 | > 2x | GPU vs CPU图片处理 | 中 |

---

## 4.2 可靠性需求

### 4.2.1 数据一致性

| 需求ID | 需求描述 | 优先级 | 实现状态 |
|--------|----------|--------|----------|
| REL-001 | 数据库事务保证数据一致性 | 高 | 🔄 规划中 |
| REL-002 | 缓存数据完整性校验 | 高 | ✅ 已实现 |
| REL-003 | 文件指纹校验 | 高 | ✅ 已实现 |

**现有实现**:

```rust
// 文件指纹校验
impl FileFingerprint {
    pub fn is_changed(&self) -> Result<bool, String> {
        let new_fingerprint = FileFingerprint::from_path(&self.path)?;
        Ok(self.hash != new_fingerprint.hash)
    }
}
```

### 4.2.2 故障恢复

| 需求ID | 需求描述 | 优先级 | 实现状态 |
|--------|----------|--------|----------|
| REL-004 | 异常崩溃后自动恢复 | 高 | ✅ 已实现 |
| REL-005 | 缓存损坏自动重建 | 中 | ✅ 已实现 |
| REL-006 | 配置损坏自动重置 | 中 | 🔄 规划中 |

**现有实现**:

```rust
// 缓存加载失败时自动重建
fn load_from_persistent_cache() -> bool {
    if let Some(manager_guard) = CacheManager::instance() {
        if let Some(manager) = manager_guard.as_ref() {
            match manager.load_from_disk() {
                Ok(cache) => {
                    if cache.songs.is_empty() {
                        return false; // 触发全量扫描
                    }
                    rebuild_memory_cache_from_persistent(&cache);
                    return true;
                }
                Err(_) => {
                    // 缓存损坏，触发重建
                    return false;
                }
            }
        }
    }
    false
}
```

### 4.2.3 网络可靠性

| 需求ID | 需求描述 | 优先级 | 实现状态 |
|--------|----------|--------|----------|
| REL-007 | 网络请求失败重试机制 | 中 | 🔄 规划中 |
| REL-008 | 网络超时处理 | 中 | 🔄 规划中 |
| REL-009 | 离线模式支持 | 中 | 🔄 规划中 |

---

## 4.3 安全性需求

### 4.3.1 数据安全

| 需求ID | 需求描述 | 优先级 | 实现状态 |
|--------|----------|--------|----------|
| SEC-001 | 用户凭证加密存储 | 高 | 🔄 规划中 |
| SEC-002 | 敏感数据不记录日志 | 中 | ✅ 已实现 |
| SEC-003 | 本地文件访问权限控制 | 中 | ✅ 已实现 |

### 4.3.2 网络安全

| 需求ID | 需求描述 | 优先级 | 实现状态 |
|--------|----------|--------|----------|
| SEC-004 | 网络通信HTTPS优先 | 高 | 🔄 规划中 |
| SEC-005 | API密钥安全存储 | 高 | 🔄 规划中 |
| SEC-006 | 请求签名验证 | 中 | 🔄 规划中 |

### 4.3.3 文件系统安全

| 需求ID | 需求描述 | 优先级 | 实现状态 |
|--------|----------|--------|----------|
| SEC-007 | 路径遍历攻击防护 | 高 | ✅ 已实现 |
| SEC-008 | 文件名安全处理 | 中 | ✅ 已实现 |

**现有实现**:

```rust
// 文件名安全处理
fn sanitize_filename(name: String) -> String {
    let sanitized: String = name
        .chars()
        .map(|c| {
            if c.is_alphanumeric() || c == '_' || c == '-' || c == '.' {
                c.to_string()
            } else {
                "_".to_string()
            }
        })
        .collect();
    
    // 限制文件名长度
    if sanitized.len() > MAX_FILENAME_LENGTH {
        let mut truncated = sanitized.chars().take(MAX_FILENAME_LENGTH).collect::<String>();
        while truncated.ends_with('.') || truncated.ends_with('_') {
            if !truncated.is_empty() {
                truncated.pop();
            } else {
                break;
            }
        }
        truncated
    } else {
        sanitized
    }
}
```

---

## 4.4 可维护性需求

### 4.4.1 代码质量

| 需求ID | 需求描述 | 优先级 | 实现状态 |
|--------|----------|--------|----------|
| MTN-001 | 模块化设计，低耦合 | 高 | ✅ 已实现 |
| MTN-002 | 单一职责原则 | 高 | ✅ 已实现 |
| MTN-003 | 依赖注入模式 | 中 | ✅ 已实现 |

**现有实现**:

```
模块化结构:
- music_tag/     # 独立的元数据解析模块
- cache_manager/ # 独立的缓存管理模块
- source/        # 抽象的数据源接口
- resources/     # 独立的资源管理模块
```

### 4.4.2 文档与注释

| 需求ID | 需求描述 | 优先级 | 实现状态 |
|--------|----------|--------|----------|
| MTN-004 | 完整代码注释和文档 | 中 | ✅ 已实现 |
| MTN-005 | API文档生成 | 低 | 🔄 规划中 |
| MTN-006 | 架构决策记录(ADR) | 低 | 🔄 规划中 |

### 4.4.3 测试覆盖

| 需求ID | 需求描述 | 优先级 | 实现状态 |
|--------|----------|--------|----------|
| MTN-007 | 单元测试覆盖率 > 70% | 中 | 🔄 规划中 |
| MTN-008 | 集成测试覆盖核心流程 | 中 | 🔄 规划中 |
| MTN-009 | 端到端测试覆盖主要功能 | 低 | 🔄 规划中 |

### 4.4.4 日志与监控

| 需求ID | 需求描述 | 优先级 | 实现状态 |
|--------|----------|--------|----------|
| MTN-010 | 日志分级记录 | 中 | ✅ 已实现 |
| MTN-011 | 性能指标监控 | 中 | ✅ 已实现 |
| MTN-012 | 错误追踪机制 | 中 | 🔄 规划中 |

**现有实现**:

```rust
// 性能监控
pub struct PerformanceMonitor {
    metrics: Mutex<HashMap<String, Vec<MetricRecord>>>,
    timers: Mutex<HashMap<String, Instant>>,
}

impl PerformanceMonitor {
    pub fn start_timer(name: &str);
    pub fn end_timer(name: &str) -> Option<u64>;
    pub fn record_metric(metric_type: MetricType, value: u64, context: String);
    pub fn get_stats() -> HashMap<String, MetricStats>;
}
```

---

## 4.5 可扩展性需求

### 4.5.1 功能扩展

| 需求ID | 需求描述 | 优先级 | 实现状态 |
|--------|----------|--------|----------|
| EXT-001 | 插件系统支持 | 低 | 🔄 规划中 |
| EXT-002 | 自定义数据源扩展 | 中 | ✅ 已实现 |
| EXT-003 | 自定义UI主题 | 低 | 🔄 规划中 |

**现有实现**:

```javascript
// 数据源抽象接口
class Source {
    async getMusicList() { throw new Error('must implement'); }
    async getAlbums() { throw new Error('must implement'); }
    // ...
}

// 可扩展的数据源管理
class SourceManager {
    registerSource(name, source) { /* ... */ }
    getSource(name) { /* ... */ }
}
```

### 4.5.2 数据扩展

| 需求ID | 需求描述 | 优先级 | 实现状态 |
|--------|----------|--------|----------|
| EXT-004 | 元数据字段扩展 | 中 | ✅ 已实现 |
| EXT-005 | 自定义标签支持 | 低 | ✅ 已实现 |
| EXT-006 | 数据库迁移机制 | 中 | 🔄 规划中 |

---

## 4.6 兼容性需求

### 4.6.1 平台兼容性

| 平台 | 支持状态 | 测试要求 |
|------|----------|----------|
| Windows 10/11 | ✅ 支持 | 主力测试平台 |
| macOS 11+ | ✅ 支持 | 定期测试 |
| Linux (Ubuntu 22.04+) | ✅ 支持 | CI测试 |

### 4.6.2 格式兼容性

| 格式类型 | 支持格式 | 优先级 |
|----------|----------|--------|
| 音频格式 | MP3, FLAC, OGG, WAV, AAC, M4A | 高 |
| 图片格式 | JPEG, PNG, WebP | 高 |
| 歌词格式 | LRC, 内嵌歌词 | 中 |
| 播放列表 | M3U, PLS (规划中) | 低 |

### 4.6.3 元数据标准兼容性

| 标准 | 支持状态 | 说明 |
|------|----------|------|
| ID3v1 | ✅ 支持 | MP3基础标签 |
| ID3v2.3/2.4 | ✅ 支持 | MP3扩展标签 |
| Vorbis Comment | ✅ 支持 | FLAC/OGG标签 |
| MP4/M4A Atoms | ✅ 支持 | M4A标签 |
| APEv2 | 🔄 规划中 | APE标签 |

---

## 4.7 用户体验需求

### 4.7.1 响应性

| 需求ID | 需求描述 | 目标值 | 优先级 |
|--------|----------|--------|--------|
| UX-001 | UI操作响应时间 | < 100ms | 高 |
| UX-002 | 列表滚动流畅度 | 60fps | 高 |
| UX-003 | 搜索输入响应 | < 50ms | 中 |

### 4.7.2 可用性

| 需求ID | 需求描述 | 优先级 |
|--------|----------|--------|
| UX-004 | 键盘快捷键支持 | 中 |
| UX-005 | 无障碍访问支持 | 低 |
| UX-006 | 多语言支持 | 低 |

### 4.7.3 视觉设计

| 需求ID | 需求描述 | 优先级 |
|--------|----------|--------|
| UX-007 | 深色模式支持 | 中 |
| UX-008 | 自定义主题 | 低 |
| UX-009 | 响应式布局 | 中 |

---

## 4.8 资源限制需求

### 4.8.1 内存限制

| 场景 | 内存限制 | 处理策略 |
|------|----------|----------|
| 空闲状态 | < 200MB | 释放非活跃资源 |
| 正常使用 | < 500MB | LRU缓存淘汰 |
| 大型音乐库 | < 1GB | 分页加载 |

### 4.8.2 磁盘限制

| 资源类型 | 默认限制 | 可配置 |
|----------|----------|--------|
| 专辑封面缓存 | 500MB | ✅ |
| 音乐库缓存 | 无限制 | ❌ |
| 日志文件 | 50MB | ✅ |

### 4.8.3 网络限制

| 场景 | 限制 | 处理策略 |
|------|------|----------|
| 单次请求超时 | 30秒 | 显示错误提示 |
| 重试次数 | 3次 | 指数退避 |
| 并发请求数 | 5个 | 请求队列 |

---

## 4.9 文档版本

| 版本 | 日期 | 修改内容 |
|------|------|----------|
| 1.0 | 2026-03-29 | 初始版本 |
